<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{  path('project_list') }}">Tasker</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Projects</a>
                    <ul class="dropdown-menu">
                        <li style="{{ app.user.selectedProject is null ? 'display: none;' : '' }}">
                            <a class="dropdown-item" href="{{ app.user.selectedProject is null ? '' : path('project_show', { 'keyCode': app.user.selectedProject.keyCode }) }}">{{ app.user.selectedProject }}</a>
                        </li>
                        <li style="{{ app.user.selectedProject is null ? 'display: none;' : '' }}"><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{  path('project_list') }}">All projects</a></li>
                        <li><a class="dropdown-item" data-bs-target="#modalCreateProject" data-bs-toggle="modal" href="#">New project</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link" data-bs-target="#modalCreateIssue" data-bs-toggle="modal" href="#">Ticket</a>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
